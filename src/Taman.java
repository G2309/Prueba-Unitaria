/*
 * Esta clase contiene el metodo que recoge el texto y por medio del metodo length regresa un valor numerico
 */
public class Taman {
	public String taman(String text) {
		int a  = text.length();
		System.out.println( "El numero de caracteres es de: " + a);
		return "";
	}
}
